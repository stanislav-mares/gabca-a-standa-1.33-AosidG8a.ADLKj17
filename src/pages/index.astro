---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import Intro from "../components/Intro.astro";
import { slideTransition } from "../utils/transitions";
---

<Layout>
    <main
        class="flex h-dvh w-full overflow-hidden relative"
        transition:animate={slideTransition}
        transition:name="main-panel"
    >
        <Intro />
        <!-- Menu container with gradient overlay -->
        <div
            class="flex items-center justify-center basis-[100%] xl:basis-[40%] 2xl:basis-[40%] grow-0 relative z-10"
        >
            <Header />
        </div>
    </main>
</Layout>
